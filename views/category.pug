extends template

block content
  .container.mt-4
      .row
        .col-12
          h1.text-center.mb-4= 'Categories'
        .col-12
        ul
        if categories.length
          each category in categories
            li
              a.text-capitalize(href=`/category/${category}`)= category
